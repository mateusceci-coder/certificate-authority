services:
  postgres:
    ports:
      - "5432:5432"

  springboot:
    ports:
      - "8080:8080"

  front:
    build:
      context: ./front
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./front:/app
      - /app/node_modules
    depends_on:
      - springboot
    networks:
      - app-network

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    depends_on:
      - front
    volumes:
      - ./nginx/nginx.dev.conf:/etc/nginx/nginx.conf:ro
